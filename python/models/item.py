class Item:
  def __init__(self, name, size, created_at):
      self.name = name
      self.size = size
      self.created_at = created_at
    
  
# user = Item(input["name"], input['size'], input["created_at"])